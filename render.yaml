services:
  - type: web
    name: geolertfrontend
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npx serve dist -s -l $PORT
    envVars:
      - key: NODE_ENV
        value: production
      - key: API_URL
        value: https://geolertbackend.onrender.com
      - key: FOUNDER_EMAIL
        value: victorychibuakunna@gmail.com